package UI.encounter_screens;

import javax.swing.*;

public interface SetCombatButtonsandLabel {

    /**
     * To fit two buttons and a label in the frame.
     */

    static final JButton attack = new JButton("Attack");
    static final JButton exit = new JButton("Exit");
    static final JLabel hp = new JLabel();
}
